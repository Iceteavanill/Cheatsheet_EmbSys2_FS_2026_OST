\section{Parallelism}

\subsection{Levels of Parallelism}
A single processor at ever-higher clock hits limits (complexity, power), so efficiency comes from parallelism:
\begin{outline}
  \1 \textbf{ILP} (instruction-level): overlap instruction stages (pipelining, superscalar, VLIW) --- gains at the same clock.
  \1 \textbf{TLP} (thread-level): several concurrent threads (own stack/registers/PC) when one program lacks independent instructions.
  \1 \textbf{DLP} (data-level) \textrightarrow{} \textbf{SIMD}: one instruction for many data elements. 
  \textbf{MIMD} \textrightarrow{} multiple instructions for multiple data (more efficient than SIMD) 
\end{outline}

\subsection{SIMD (single instruction, multiple data)}
% GOAL V10 • You can explain how SIMD instructions lead to improved performance of applications like signal processing, machine learning or real-time data handling on microcontrollers.

\begin{minipage}[t]{0.48\linewidth}
    \vspace*{0pt}
    Data is stacked in a single register because the register can fit multiple data elements (1 32 bit register can fit 4 8 bit data elements).
    An operation is made on all of them in parallel in a single instruction.
    The ALU can configure \textbf{lanes} for individual data paths, depending on the width and ALU capabilities.
\end{minipage}\hfill
\begin{minipage}[t]{0.5\linewidth}
    \vspace*{0pt}
    \includegraphics[width=\columnwidth]{images/PDF/adder_groups_simd.pdf}
\end{minipage}
% GOAL L10 • You can apply the concepts of loop unrolling and partial loop unrolling.

Note: \textbf{Loop unrolling} replicates the body (e.g.\ 4 MACs/iter) to cut loop-overhead per useful op.
\emph{partial} unrolling handles a remainder separately.
\subsubsection{Code implementation example of a dotproduct}
% GOAL L10 • You can utilize SIMD instructions to speed up basic matrix and vector calculations.
% GOAL L10 • You can work with the CMSIS-Core and CMSIS DSP library on Arm Cortex microcontrollers to realize the vector and matrix operations.
\lstinputlisting[lastline=1]{code/simd.cpp}

\begin{minipage}[t]{0.4\linewidth}
    \lstinputlisting[linerange=2-5]{code/simd.cpp}
    This implementation is simple but slow.
    The SIMD however needs some casts to acquire the separate memory blocks
\end{minipage}
\begin{minipage}[t]{0.6\linewidth}
    \lstinputlisting[linerange=7-15]{code/simd.cpp}
\end{minipage}
\begin{lstlisting}
}
\end{lstlisting}

\subsection{Pipelining}
% GOAL V11 • You can explain how pipelining and its variations like superscalar or VLIW architectures work.

Split a task into \textbf{stages} processed in overlap (assembly line). 
\begin{itemize}
    \item \textbf{Fetch:} Load instruction from program memory
    \item \textbf{Decode:} decode instruction, load address and operand from registers
    \item \textbf{Execute:} Execute task of instruction or calculate address for following memory access
    \item \textbf{Memory Access:} Access memory (inkl. Cache) if used in instruction, else it is skipped
    \item \textbf{Write Back:} Write back to registers 
\end{itemize}
\textbf{Pipeline registers} hold data between stages (transparent to the user). Ideal: one finished instruction \emph{per cycle}. 
\[ \text{Speedup} = \frac{\text{CPI unpipelined}}{1 + \text{stall cycles per instruction}} = \frac{\text{Pipeline depth}}{1 + \text{stall cycles per instruction}} \]
Limits: bus access times, pipeline-register delay, hazards.

\includegraphics[width=\columnwidth]{images/PDF/pipeline.pdf}

\subsection{Pipeline Hazards}
% GOAL V11 • You can assess challenges associated with pipelines like hazards and describe possible solutions.
Three types: \textbf{structural} (resource conflicts), \textbf{data}, \textbf{control}.
\begin{outline}
  \1 \textbf{Data:} \emph{RAW} (read-after-write, the true/most common dependency --- solved by \textbf{data forwarding} or compiler scheduling), \emph{WAW} (write order, compiler), \emph{WAR} (anti-dependency, relevant out-of-order).
  \1 \textbf{Control:} branches (\(\sim\)20\% of ops, \(\sim\)66\% of conditional ones taken). 
  Solutions: \emph{look-ahead resolution} (compute target in parallel), \emph{delay slot}, \emph{branch prediction}.
\end{outline}
Interrupts/exceptions are handled by saving state \textrightarrow{} ISR \textrightarrow{} resume (extra HW, watch delays).

\subsection{Superscalar \& VLIW}
\textbf{Superscalar:} duplicated functional units issue $>1$ instruction/cycle ($\text{CPI}<1$); 
abandons strict Von-Neumann order \textrightarrow{} \textbf{out-of-order} execution, shifting focus from \emph{instruction} to 
\emph{result} sequentiality (hazards more severe, solve at compile time). \textbf{Superpipeline:} finer, shorter stages at higher clock (mixes exist). 
\textbf{VLIW:} several operations packed in one long instruction word, scheduled by the \textbf{compiler} (avoids hazards, simplifies runtime HW). 
\textbf{Hyper-threading (SMT):} hardware multithreading $+$ superscalar pipelining (combines TLP $+$ ILP).

\subsection{Flynn's Taxonomy}{Klassierung nach Flynn}
% GOAL V11 • You can describe and classify computing platforms according to their (potentially different) architectural characteristics using Flynn’s taxonomy.
% GOAL V11 • You can recognize the specific type of a multi-processor architecture and name the underlying mechanisms and primitives.
% \begin{ctabular}{l|cc}
%  & \textbf{single data} & \textbf{multiple data} \\ \hline
% \textbf{single instr.} & SISD & SIMD \\
% \textbf{multiple instr.} & MISD & MIMD \\
% \end{ctabular}
\includegraphics[width=\columnwidth]{images/PDF/flynn.pdf}

\textbf{SISD:} Von-Neumann / Harvard. 
\textbf{SIMD:} vector/array processors. 
\textbf{MISD:} no real machines (akin to systolic arrays). 
\textbf{MIMD:} multiprocessors, GPUs (multi-threaded SIMD), NPU/TPU. 
\textbf{Von-Neumann bottleneck:} one bus for instructions $+$ data; 
\textbf{Harvard} (separate I/D buses or caches) relieves it.


\subsection{Multiprocessors (MIMD)}% TODO maybe uf homogen / heterogen igah -> interrupt uf anderem core
\begin{outline}
  \1 \textbf{Coupling:} \emph{tightly coupled} (shared memory) vs.\ \emph{loosely coupled} (message passing over fast networks).
  \1 \textbf{Task division:} \textbf{AMP} (one master controls others, own address space per CPU) vs.\ \textbf{SMP} (every CPU can do any task, shared address space).
  \1 \textbf{UMA} (uniform access via bus) vs.\ \textbf{NUMA} (local memory per CPU, different access times).
\end{outline}

\subsection{Performance Analysis}
% GOAL L10 • You can use a cycle counter to analyze the speed of a function.

% The execution time of a program can be calculated with
% \[
% \text{Time}
% =
% \frac{\text{Instructions}}{\text{Program}}
% \times
% \frac{\text{Clock cycles}}{\text{Instruction}}
% \times
% \frac{\text{Seconds}}{\text{Clock cycle}}.
% \]
% Note : clockcycles per instruction varies from \textbf{CISC} to \textbf{RISK}. Typically, a mean value is used.
See section \ref{sec:execution_time} for base model.

\subsubsection{Improvements in execution time}
\[
T_{\text{App}} 
= 
\frac{\text{Thr}}{\text{App}}\cdot
\frac{\text{Op}}{\text{Thr}}\cdot
\frac{\text{Instr}}{\text{Op}}\cdot
\frac{\text{Clk}}{\text{Instr}}\cdot
\frac{\text{time}}{\text{Clk}}
= 
\mathrm{TPA}\cdot
\mathrm{OPT}\cdot
\mathrm{IPO}\cdot
\mathrm{CPI}\cdot
\mathrm{TPC}
\]
\begin{center}
\begin{tabular}{p{0.07\linewidth}p{0.49\linewidth}p{0.3\linewidth}}
\hline
\textbf{factor} & \textbf{meaning / parallel form} & \textbf{limit} \\
\hline
TPA & Threads per Application (Thread-Level Parallelism) & Compiler technology \\
OPT & Operations per Thread (typical compiler) & -- \\
IPO & Instructions per Operation (ILP, VLIW); 1 (CISC), $<1$ (VLIW) & Compiler technology \\
CPI & Clocks per Instruction (ILP superscalar, HW) & memory access, ILP \\
TPC & Time per Clock (process node dependent) & Heating, min.\ pipeline length \\
\hline
\end{tabular}
\end{center}

\subsubsection{Amdahl's Law (strong scaling)}
\begin{itemize}
    \item \textbf{Rule:} The performance increase from an improvement is limited by the proportion of time the improved part is used.
    \item Application to multi-processors: Improvement $=$ parallelization; utilization rate $=$ degree of parallelizability.
    \item Guiding principles: \emph{make common case fast}; \emph{rule of diminishing returns}; another part of the system can become the new bottleneck.
\end{itemize}

\subsubsection{Derivation of Speed-up}
\textit{Notation:} 
$A$ Workload (steps); 
$L_{\text{SISD}}$, $L_{\text{MIMD}}$ Performance (steps/time); 
$n$ number of processors; 
$s$ sequential, 
$p$ parallel part with $(s+p)\cdot A = A$, so $s+p=1$, $p=1-s$.
\begin{align*}
T_m &= T_s + T_p = \frac{s\cdot A}{L_{\text{SISD}}} + \frac{p\cdot A}{n\cdot L_{\text{SISD}}} = \frac{A}{L_{\text{SISD}}}\left(s+\frac{p}{n}\right)\\
\overline{L_{\text{MIMD}}} &= \frac{A}{T_m} = L_{\text{SISD}}\cdot\frac{1}{s+\frac{1-s}{n}} = \frac{L_{\text{SISD}}}{s+\frac{1-s}{n}}
\end{align*}
\[
\boxed{\;\text{Speedup} = \frac{\overline{L_{\text{MIMD}}}}{L_{\text{SISD}}} = \frac{1}{s+\dfrac{1-s}{n}}\;}
\]
% Grenzfall $n\to\infty$: $\text{Speedup}\to 1/s$ (harte obere Schranke durch den seriellen Anteil).

\par{Examples}% TODO EXPAND!!
\textbf{1}: $s=0.1$, $p=0.9$:
\begin{itemize}
    \item $n=1$: $1$; \quad $n=4$: $1/(0.1+0.225)\approx 3.08$;
    \item $n=15$: $1/(0.1+0.06)=6.25$; \quad $n\to\infty$: $1/0.1=10$.
\end{itemize}
\textbf{2 (multiple  paralell parts):} $32$ Units, $s=9\%$, $4$-fach parallel $p_4=17\%$, voll parallelisierbar $p_\infty=74\%$. Verallgemeinert:
\begin{equation}
\text{Speedup}=\frac{1}{s+\dfrac{p_4}{4}+\dfrac{p_\infty}{n}}
\end{equation}
\begin{itemize}
    \item $n=32$: $1/(0.09+0.0425+0.0231)\approx 6.43$.
    \item $n=16$: $1/(0.09+0.0425+0.0463)\approx 5.59$.
    \item Verlangsamungsfaktor $32\!\to\!16$: $6.43/5.59\approx 1.15$ (Programm läuft ca.\ $15\%$ langsamer).
\end{itemize}

\subsection{Gustafson's Law (Weak Scaling)}

\subsection{Roofline Model}


\begin{minipage}[t]{0.49\linewidth}
    \vspace*{0pt}
    Code it \textbf{compute bound} if it has high operational intensity, \textbf{memory bound} if it has low operational intensity.
\end{minipage}\hfill
\begin{minipage}[t]{0.5\linewidth}
    \vspace*{0pt}
    \input{images/tikz/roofline.tex}
\end{minipage}