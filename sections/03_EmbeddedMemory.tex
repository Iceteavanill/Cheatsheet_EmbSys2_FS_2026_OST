\section{Embedded Memory}

\subsection{Memory Types}
% GOAL V05 • You are familiar with the basic types of embedded memory.
  \vspace{-0.1cm}
  \begin{center}
    \begin{tabular}{llll}
      \toprule
      \textbf{Type} & \textbf{Volatile} & \textbf{Writable}   & \textbf{Typical Use} \\ \midrule
      SRAM          & yes               & yes                 & Cache, Stack, Heap, Variables \\
      DRAM          & yes               & yes                 & Main memory (typ.\ external) \\
      ROM           & no                & no                  & Boot code, Constants \\
      Flash         & no                & yes (block/sector erase) & Program code, Persistent data \\
      EEPROM        & no                & yes (byte-wise write)    & Configuration, Calibration data \\
      \bottomrule
    \end{tabular}
  \end{center}
  \vspace{-0.1cm}
  \textit{SRAM is fast, requires no refresh; DRAM is denser, cheaper, and requires periodic refresh.}

\subsection{Memory Model and Memory Map}
% GOAL V05 • You are familiar with the basic types of embedded memory, the memory model and memory map as typically used in a microcontroller.
% GOAL L08 • You know the memory model of a microcontroller and how to find the different memory locations.

  \begin{minipage}[t]{0.58\linewidth}
    Memory locations are defined by the microcontroller memory map and can be found in the reference manual (Memory Map chapter) or development tools (e.g. STM32CubeIDE Build Analyzer / linker map).
  
    Cortex-M systems use a flat 32-bit address space ($2^{32} = \qty{4}{GiB}$) with memory-mapped Flash, RAM, and peripheral regions.
    External memories (e.g. QSPI Flash) are also mapped into the address space via the memory map.
  \end{minipage}\hfill
  \begin{minipage}[t]{0.38\linewidth}
    \vspace{0pt}
    \includegraphics[width=\linewidth]{images/PDF/EmbeddedMemory_MemoryMapCortexM.pdf}
  \end{minipage}

  \begin{center}
    \begin{tabular}{lll}
      \toprule
      \textbf{Section} & \textbf{Location} & \textbf{Content} \\ \midrule
      \texttt{.text}   & Flash & executable code \\
      \texttt{.rodata} & Flash & Read-only data, constants (\texttt{const}) \\
      \texttt{.data}   & RAM (init from Flash) & Initialized global/static variables \\
      \texttt{.bss}    & RAM & Zero-initialized global/static variables \\
      Heap             & RAM $\uparrow$ & Dynamic allocation (\texttt{new}/\texttt{pvPortMalloc}) \\
      Stack            & RAM $\downarrow$ & Local variables, function calls \\ \bottomrule
    \end{tabular}
  \end{center}

\subsection{FreeRTOS Memory Model}
% GOAL V05 • You can describe the memory usage regarding FreeRTOS task stacks and heap implementations.
% GOAL L06 • You can understand where and how a task is placed in memory.
% GOAL L06 • You can assess and estimate the memory usage of a task.

  \subsubsection{Task Stack and Heap Usage}
  
  \begin{minipage}[t]{0.6\linewidth}
    \vspace{0pt}
    \begin{itemize}
      \item Each FreeRTOS task consists of:
      \begin{itemize}
          \item a \textbf{stack} (local variables, context)
          \item a \textbf{Task Control Block (TCB)}
      \end{itemize}
  
      \item With dynamic allocation:
      \begin{itemize}
          \item Stack + TCB are allocated from the \textbf{FreeRTOS heap (ucHeap)}
          \item Allocation via \texttt{pvPortMalloc}
      \end{itemize}
  
      \item Stack usage can be estimated using:
      \begin{itemize}
        \item \texttt{uxTaskGetStackHighWaterMark()} returns the minimum unused stack space (words).
      \end{itemize}
    \end{itemize}
  
      \begin{itemize}
          \item FreeRTOS heap is a static array (\texttt{ucHeap})
          \item Used for tasks, queues, semaphores
      \end{itemize}

\end{minipage}\hfill
\begin{minipage}[t]{0.38\linewidth}
  \vspace{0pt}
  \begin{tikzpicture}[node distance=1cm]

  \node (task) [draw, rectangle] {Task};

  \node (stack) [draw, rectangle, below left=of task] {Stack};
  \node (tcb)   [draw, rectangle, below right=of task] {TCB};

  \node (heap)  [draw, rectangle, below=1.5cm of task] {FreeRTOS Heap (ucHeap)};

  \node (bss)   [draw, rectangle, below=of heap] {.bss section (RAM)};

  \draw[->] (task) -- (stack);
  \draw[->] (task) -- (tcb);

  \draw[->] (stack) -- (heap);
  \draw[->] (tcb) -- (heap);

  \draw[->] (heap) -- (bss);

  \end{tikzpicture}
\end{minipage}


\subsubsection{Heap implementations}

Heap implementation 1 to 5 are provided by FreeRTOS.

\includegraphics[width=\columnwidth]{images/PDF/heaps.pdf}

\begin{tabular}{ll}
  \toprule
  \textbf{Heap implementation} & \textbf{Key feature} \\
  \midrule
  \textbf{heap\_1} & \texttt{malloc} only (no \texttt{free}) \\
  \textbf{heap\_2} & \texttt{malloc}/\texttt{free} \\
  \textbf{heap\_3} & Wrapper for standard C allocator \\
  \textbf{heap\_4} & Coalescing allocator (recommended) \\
  \textbf{heap\_5} & Multiple memory regions \\
  \bottomrule
\end{tabular}

\subsection{Static and Dynamic Allocation}
  % GOAL L06 • You can explain the pros and cons of static/dynamic allocation.
  % GOAL L06 • You can use FreeRTOS with and without heap.

  \begin{tabular}{p{0.23\linewidth} p{0.3\linewidth} p{0.34\linewidth}}
    \toprule
    \textbf{Aspect}             & \textbf{Static Allocation}  & \textbf{Dynamic Allocation} \\
    \midrule
    \textbf{Memory allocation}  & at compile time             & at runtime (heap) \\ \midrule
    \textbf{Heap usage}         & No                          & FreeRTOS heap\\ && (\texttt{pvPortMalloc} / \texttt{new}) \\ \midrule
    \textbf{Deterministic}      & Yes                         & No (allocation overhead) \\
    \textbf{Memory size}        & Fixed                       & Flexible \\
    \textbf{Memory risks}       & No fragmentation            & Fragmentation possible \\ \midrule
    \textbf{Example (C++)}      & \texttt{\small RedTaskClass task(...);} & \texttt{\small RedTaskClass* task = new RedTaskClass(...);} \\
    \bottomrule
  \end{tabular}

\subsection{Fragmentation}
% GOAL V05 • You can explain internal / external fragmentation and coalescing / free-space management.
% GOAL L06 • You can describe what memory fragmentation is.

\begin{minipage}[t]{0.73\linewidth}
  \subsubsection{Internal Fragmentation}
  
  If a reserved block is \textbf{larger} than the \textbf{actually used} data.
  The unused space inside the block is wasted.
  
  \subsubsection{External Fragmentation}
  Free memory is split into many small \textbf{non-contiguous} blocks.
  Total free space maybe sufficient, but no single block is large enough for a request.
\end{minipage}\hfill
\begin{minipage}[t]{0.25\linewidth}
  \vspace{0pt}
  \includegraphics[width=\linewidth]{images/PDF/fragmentation.pdf}
\end{minipage}

The external memory fragmentation can be calculated By
\[
\text{External Memory Fragmentation (in percent)}
=
1
-
\frac{\text{size of biggest free memory block}}{\text{Size of total free memory}}
\]
where the value $0$ means all free memory is in one contiguous block (no fragmentation) and values approaching $1$ indicate severe fragmentation.
A \textit{lower} fragmentation is better.

\subsubsection{Memory Coalescing and Free Space Management}

Free space is tracked with a \textbf{free list}.
If 2 neighbouring blocks are free, they can be united to one single large empty block \textrightarrow{} coalescing.
This reduces \emph{external} fragmentation.

\subsection{Memory Analysis}
% GOAL L06 • You can apply watermarking to analyze memory usage.

To analyse heap memory usage, \textbf{watermarking} can be used.
During allocation, the heap implementation marks the allocated memory segment with an identifier.
This lets one visualize the heap usage.
Allocated but unused areas can be visually found by examining the memory contents.

\subsubsection{Memory Usage Estimation}

The memory usage of a task is not very clear and depends on many factors.
Mainly, it is unknown how big the stack grows
\begin{itemize}
  \item The function call nesting depth
  \item The number and size of function scope variable declarations
  \item The number of function parameters
  \item The processor architecture
  \item \dots
\end{itemize}
To estimate the typical memory usage, an arbitrary stack size can be used at first.
RTOS keeps track of minimum free stack size of a task.
If it shrinks to zero during runtime, not enough memory for the stack is allocated. 
